/*
 * config_secret.h.example — copy to config_secret.h (git-ignored) and fill in.
 * Node A holds the real BMS PIN (spec §12); keep it out of version control.
 */
#ifndef CONFIG_SECRET_H
#define CONFIG_SECRET_H

#define WIFI_SSID      "your-ssid"
#define WIFI_PASS      "your-pass"

#define MQTT_URI       "mqtt://192.168.2.5:1883"
#define MQTT_USER      "jkbridge"
#define MQTT_PASS      "changeme"

/* Real BMS login PIN(s). PINs seen in the wild: 1234 / 123456 (spec §10, O-5). */
#define JK_LOGIN_PIN   "123456"

#endif /* CONFIG_SECRET_H */
