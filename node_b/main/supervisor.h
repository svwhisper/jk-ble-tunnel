/*
 * supervisor.h — Node B watchdog, connection-slot / RAM monitoring,
 * cache-invalidation housekeeping (spec §5).
 */
#ifndef SUPERVISOR_H
#define SUPERVISOR_H
void supervisor_start(void);
#endif
